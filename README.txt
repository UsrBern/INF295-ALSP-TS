Como compilar y ejecutar el programa:

En una terminal abierta en la carpeta del proyecto, ejecutar los siguientes comandos:

make
./ALSP

Para limpiar los archivos generados por la compilación, ejecutar:

make clean